# tg_bot_for_reaviz

Telegram-бот для прорешивания тестов из Excel-файлов: `anatomy_test.xlsx` (анатомия)
и `histology_test.xlsx` (гистология).

## Что умеет

- предлагает выбрать предмет: `Анатомия` или `Гистология` (кнопка `Сменить предмет`)
- показывает кнопку `Начать тест`
- предлагает выбрать количество вопросов
- берёт случайные вопросы из Excel-файла
- позволяет выбрать конкретные номера вопросов, например `10-15`
- показывает варианты ответов кнопками
- сразу сообщает, верно или неверно
- при ошибке показывает правильный ответ текстом, а не только буквой
- поддерживает вопросы с несколькими правильными ответами
- поддерживает задания на соответствие и последовательность
- умеет останавливать тест

## Запуск

1. Установить зависимости:

```powershell
python -m pip install -r requirements.txt
```

2. Задать токен бота:

```powershell
$env:TELEGRAM_BOT_TOKEN="ВАШ_ТОКЕН"
```

3. Запустить:

```powershell
python bot.py
```

## Структура проекта

- `bot.py` — тонкая точка входа: настраивает логирование и запускает приложение.
- `reaviz_bot/config.py` — загрузка переменных окружения и путей к Excel-файлам по предметам.
- `reaviz_bot/application.py` — сборка Telegram Application и регистрация обработчиков.
- `reaviz_bot/models.py` — доменные модели вопроса и тестовой сессии.
- `reaviz_bot/question_repository.py` — загрузка вопросов из Excel.
- `reaviz_bot/question_parser.py` — разбор правильных ответов и типов вопросов.
- `reaviz_bot/question_bank.py` — выбор случайных вопросов.
- `reaviz_bot/evaluator.py` — проверка ответов пользователя.
- `reaviz_bot/keyboards.py` — фабрика Telegram-клавиатур.
- `reaviz_bot/message_formatter.py` — форматирование текста вопросов.
- `reaviz_bot/session_store.py` — хранение сессии в `chat_data`.
- `reaviz_bot/handlers.py` — Telegram-обработчики команд, текста и callback-кнопок.

## Формат Excel

- 1 столбец: вопрос
- следующие столбцы: варианты ответов
- столбец с заголовком `Правильный ответ`: правильный ответ
- столбцы после `Правильный ответ` игнорируются (можно держать там свои пометки)

### Логика правильного ответа

- одна буква: один правильный ответ
- несколько букв: несколько правильных ответов
- буквы с цифрами или только цифры: задание на соответствие или последовательность
